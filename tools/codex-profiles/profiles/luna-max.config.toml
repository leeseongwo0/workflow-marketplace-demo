# Copy to ~/.codex/luna-max.config.toml
# If this Codex CLI build rejects "max", use the xhigh fallback or /model.
model = "gpt-5.6-luna"
model_reasoning_effort = "max"
model_verbosity = "low"
approval_policy = "on-request"
